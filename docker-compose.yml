version: '3.8'

services:
  app:
    build: .
    container_name: pwa-task-manager
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Montar certificados Let's Encrypt generados por Certbot en el host
      - /etc/letsencrypt:/etc/letsencrypt:ro
    restart: unless-stopped
